.playlist__item.thumbnail
  = link_to playlist do 
    .links-container
      - playlist.links.each do |link|
        .playlist__thumb= image_tag link.thumbnail_url
    %h3.playlist__title= playlist.name