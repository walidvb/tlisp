{
  "proxy": "http://localhost:3001",
  "dependencies": {
    "react": "^16.2.0",
    "select2": "^4.0.6-rc.0"
  },
  "scripts": {
    "deploy": "cp -a client/build/. public/",
    "postinstall": "npm run deploy && echo 'Client built!'"
  }
}
