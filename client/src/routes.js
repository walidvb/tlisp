const routes = {
    api: {
        links: {
            index: '/api/links.json',
            filters: '/api/filters.json',
        },
    },
    links: {
        new: '/tracks/new',
    }
}

export default routes